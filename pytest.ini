[pytest]
; Agrega la raiz del proyecto al PYTHONPATH automaticamente, para que
; "from app.domain import ..." funcione sin importar desde que carpeta
; se ejecute "pytest" (ni falta configurar variables de entorno a mano).
pythonpath = .
testpaths = tests
